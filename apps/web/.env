# Build/dev-time config (safe to commit - nothing here is a secret; secrets go through
# `wrangler secret put`, never files). Variables must also be declared in src/env.ts -
# that file is the typed SSOT; this file just supplies values.
REALTIME_ORIGIN=http://localhost:8787
