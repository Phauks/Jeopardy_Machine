# Status

> Stamp: verified against the rebuilt front door and the game-screen setup + console join panel, merged onto M6's buzz latency compensation, 2026-08-19 UTC. Prefer the reproduce-commands over the copied numbers - if they disagree, this file is stale; fix it in the same commit as whatever changed.

## Live state

| Claim                                                                                                                                    | Reproduce                                                                                                                                                                           |
| ---------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Workspace installs clean (pnpm 10.33.0, Node 22)                                                                                         | `pnpm install --frozen-lockfile`                                                                                                                                                    |
| All tests green: 5 packages, 1439 tests (259 protocol, 173 engine, 14 bots, 876 web, 117 realtime-in-workerd)                            | `pnpm test`                                                                                                                                                                         |
| Typechecks green (svelte-check + tsc, incl. service-worker project)                                                                      | `pnpm check`                                                                                                                                                                        |
| Lint + format gates green                                                                                                                | `pnpm lint && pnpm exec vp fmt --check`                                                                                                                                             |
| Web app builds through adapter-cloudflare; realtime bundles via dry-run deploy                                                           | `pnpm build`                                                                                                                                                                        |
| M1 document layer live: 4 portable formats parse through one entry point                                                                 | `pnpm -F @jeopardy/protocol test` (`parsePortableDocument`; migration machinery proven by the fixture-gated example migration)                                                      |
| M2 engine live: pure state machine, matrix rows cited in test names, undo-by-replay, seeded determinism                                  | `pnpm -F @jeopardy/engine test` (fixture catalog under `packages/engine/fixtures/` replays twice per scenario)                                                                      |
| A full game plays locally with NO server (M2 exit criteria)                                                                              | `pnpm dev`, open http://localhost:5173/dev/hotseat (headless proof: `apps/web/src/lib/hotseat/sample-game.test.ts` drives 60 cells + final to game-over)                            |
| M3 room server live: explicit create, engine feed, sessions, teams, alarms - proven against real eviction                                | `pnpm -F @jeopardy/realtime test` (workerd suite incl. `evictDurableObject` mid-game, expiry via `runDurableObjectAlarm`, bot players on real sockets)                              |
| Single-origin WS passthrough works on the pinned kit/adapter (M3 week-1 risk: resolved, no fallback needed)                              | build + multi-config `wrangler dev`, then `node apps/web/scripts/prove-single-origin.mjs` (decision addendum: docs/decisions/2026-08-13-single-origin-binding.md)                   |
| M3 exit criteria: real-browser phones + display + host, deterministic buzz race, roster sync, harness flow                               | `pnpm -F @jeopardy/web test:e2e` (5 Playwright multi-context tests across two suites; needs a local chromium - docs/DEVELOPMENT.md "End to end")                                    |
| The play surfaces talk to REAL rooms: a created code opens a socket to its `GameRoomDO`, `DUMYX` and `?sim` stay the local simulation    | `pnpm -F @jeopardy/web test` (`room/ws-room-store.test.ts` - every server message through the real parser into a `RoomView`, plus the store choice by code)                         |
| Bot players speak the real room protocol (seeded, reproducible); a bots-only game plays to game-over                                     | `pnpm -F @jeopardy/bots test`; live: `pnpm -F @jeopardy/bots bots -- --origin http://localhost:8788 --create --count 5 --host` against the single-origin dev loop                   |
| Room instrument panel: create/delete rooms, session room list, join roles, DO inspector, lobby watch, refusal probes                     | `pnpm dev:rooms`, open http://localhost:8788/dev/rooms (`/dev/echo` redirects there)                                                                                                |
| The registry says why a lobby is empty (`ok` / `no-binding` / `no-table` / `error`) on the listing, the create response and /api/version | `pnpm -F @jeopardy/web test` (`rooms-endpoint.test.ts`); live: `curl localhost:8788/api/version`                                                                                    |
| Host-authenticated room ops: `GET /api/rooms/<CODE>` inspects the DO, `DELETE` closes it and delists the row                             | `pnpm -F @jeopardy/realtime exec vitest run test/diagnostics.test.ts` (incl. the redaction gate: no tokens, password or clue text)                                                  |
| Room visibility + passwords live: listing and entry are independent axes, PBKDF2-verified in the DO, rate-limited per connection         | `pnpm -F @jeopardy/realtime exec vitest run test/passwords.test.ts`                                                                                                                 |
| The public lobby lists live public rooms from D1 (registry rows are a cache; the DO refuses dead rooms regardless)                       | `pnpm -F @jeopardy/realtime exec vitest run test/registry.test.ts` (real D1 + the web app's migration); browser: `/` and the harness's "List public rooms"                          |
| The lobby row projects BOTH budgets: players against the room's own `maxPlayers`, and the audience the DO counts from live connections   | `pnpm -F @jeopardy/realtime exec vitest run test/registry.test.ts`; `pnpm -F @jeopardy/web test` (`server/room-registry.test.ts` incl. the migration column gate)                   |
| Streamer mode is honoured where it matters: with `hideJoinCode` on, the code, QR and join URL are absent from the display's MARKUP       | `pnpm -F @jeopardy/web test` (`room-settings-surfaces.test.ts`)                                                                                                                     |
| ...and INVERTED on the console, deliberately: the host's own join panel keeps the code, the QR and the link, and says they are hidden    | `pnpm -F @jeopardy/web test` (`host-console.setup.test.ts`, the streamer-mode describe - both sides asserted in one file)                                                           |
| The console opens the game screen, tracks it (never-opened / open / closed), and warns before starting a game with none attached         | `pnpm -F @jeopardy/web test` (`game-screen.test.ts`, `host-console.setup.test.ts`); live: `/room/DUMYX/host`                                                                        |
| The theme document carries the two presentation slots (`environment`, `staging`); unbuilt scenery and unknown ids resolve, never blank   | `pnpm -F @jeopardy/protocol test` (`theme/theme.test.ts`); `pnpm -F @jeopardy/web test` (`theme/theme-slots.test.ts`)                                                               |
| Clue content, host pause/force-expire, and the polite close/kick reasons are on the wire (M4 reconcile)                                  | `pnpm -F @jeopardy/realtime exec vitest run test/host-controls.test.ts`                                                                                                             |
| No two staged-lobby stations overlap at any team count, and nobody stands inside anybody in the holding area                             | `pnpm -F @jeopardy/web test` (`staging/staging-layout.test.ts` - separating-axis over 14 counts x 2 canvas shapes x both themes)                                                    |
| The holding area says what it is in words, on BOTH the 3D stage and the 2D fallback, and every station lists its crew beneath it         | `pnpm -F @jeopardy/web test` (`staging/staged-lobby.states.test.ts`, `staging/staging-copy.test.ts`, `diorama/motion-guardrails.gate.test.ts`)                                      |
| Display and console type scales move independently, persist per device, and never touch the room                                         | `pnpm -F @jeopardy/web test` (`host-settings/host-settings.test.ts`); live: `/room/DUMYX/host?settings`                                                                             |
| The whole C4 host loop runs - select, arm, buzz, judge, rebound, no-takers, reopen, wager, final wizard, undo, override, pause           | `pnpm -F @jeopardy/web test` (`room/host-loop.verify.test.ts` - asserted at store level AND against the rendered console)                                                           |
| M6 buzz fairness: the slow phone that pressed FIRST beats the fast phone that pressed later, and a lying client gains nothing by it      | `pnpm -F @jeopardy/realtime exec vitest run test/buzz-fairness.test.ts`; the arithmetic and its adversarial cases: `pnpm -F @jeopardy/protocol test` (`room/buzz-fairness.test.ts`) |
| The fairness claim is MEASURED, not asserted: same seeded field, compensation on vs off                                                  | `pnpm dev:rooms`, then `pnpm -F @jeopardy/bots bots -- --create --race 4 --seed demo2` and the same command with `--no-compensation` (4/4 fastest thumb vs 0/4)                     |
| The web surfaces hold up their end of it: ack first, elapsed measured from the PAINT, no claim when nothing was painted                  | `pnpm -F @jeopardy/web test` (`room/ws-room-store.test.ts`, "the client half of buzz latency compensation")                                                                         |
| A reconnecting phone or a reopened console comes back to a RUNNING countdown, not a frozen one                                           | `pnpm -F @jeopardy/web test` (same file, the snapshot-timers case); `pnpm -F @jeopardy/realtime exec vitest run test/reconnection.test.ts`                                          |
| Early-buzz penalty (#12) end to end: private feedback with the deadline, re-triggered by mashing, team-wide under #36                    | `pnpm -F @jeopardy/realtime exec vitest run test/early-lockout.test.ts`                                                                                                             |
| The A5 reconnection promises and the C6 host resume hold against the DO (incl. eviction mid-armed-window)                                | `pnpm -F @jeopardy/realtime exec vitest run test/reconnection.test.ts test/buzz-fairness.test.ts`                                                                                   |
| Generated settings reference is current (registry-derived, drift-gated)                                                                  | `pnpm -F @jeopardy/protocol generate:settings-docs && git diff --exit-code docs/reference/settings.md`                                                                              |

## For M4 (the client store contract)

The surfaces build on what the DO already speaks (all shapes in `packages/protocol/src/room/`):

- Connect to `wss://<origin>/room/<CODE>/ws`, send `join` (or `resume` with the sessionStorage token), receive `welcome` + `snapshot`, then apply `event` batches in order; `stateVersion` gaps mean a missed message - send `sync` for a fresh snapshot.
- `snapshot.game` is the engine `GameState` redacted per role (cast to the engine type); roster/team payloads arrive whole on every change - no diffing client-side.
- Room audio keys off the room-level `buzz-won` message ONLY (it carries the resolved team-scoped sound id); `buzz-rejected` is private per-phone feedback (the DO sends it to that one connection; it is never in the event stream).
- **Buzzing (M6)**: on `arm-window`, note the local render time, send `arm-ack { armId }` immediately, and attach `timing: { armId, elapsedMs }` to the buzz action - that is the whole client contract for latency compensation, and no clock synchronization is implied. `arm.compensationMs` is how long the room may hold the race, so optimistic feedback can be sized against the real wait. A client that skips it is ranked by arrival.
- `snapshot.timers` carries every running countdown as REMAINING ms (empty while paused or idle) - that is what a surface reconnecting mid-clue renders instead of guessing.
- Engine timer bars render from `timer-set` events; ordinary expiries are server-driven (the DO's alarm book) - clients never dispatch timeout actions. The host console's "skip the wait" is the `expire-timer` message, and its pause button is `set-pause` (both host-only; pause freezes every running timer at the time it had left).
- Authored clue text arrives on `clue-content` (and on `snapshot.clueContent` while a clue is open), redacted per role: the ANSWER is host-only; the prompt reaches phones only when the room's `clueTextOnPhones` setting is on.
- `room-closed` reasons are `expired` / `host-closed` / `kicked` - show copy per reason (user-flows A5's polite screen), never a generic disconnect.
- In teams mode, a player who never picked a team is seated as a solo team of one at start-game (and on late join) - clients do not need to block the host on it.
- The dev sim panel spawns `@jeopardy/bots` bots; the compact game spec in `room/create` is its room-spinning path.

## Not yet true (so nobody assumes it)

- **Nothing is deployed.** Both Workers have never left local dev; first deploy is a manual owner step (docs/cloudflare-setup.md). CI has no deploy jobs on purpose.
- No R2 media flows exist yet; the R2 binding sits unused by code. D1 is in use for the room registry ONLY, and only once the owner applies `apps/web/migrations` by hand (docs/cloudflare-setup.md §2a - **the file was rewritten twice and needs ONE re-apply covering both the listing rename and the spectator columns**) - until then the lobby answers empty (now with `registry.reason = "no-table"` saying so) and rooms work by code as before. **The migration has not been applied to any deployed environment yet** - that is the known cause of an empty lobby on the live site.
- The registry can drift from room truth (best-effort writes, coalesced roster counts). That is the designed failure mode: rows are a cache, the listing query filters on liveness, and the DO refuses dead rooms whatever a row says.
- The M1 visual editor and IndexedDB repositories do not exist yet - the document schemas are consumable, but nothing authors or persists them (M1 phase 2). The M1 exit-criteria run is blocked on that.
- The play surfaces exist but are NOT yet connected to the room server: every one of them runs on the local-sim store, so each browser tab is its own isolated simulated room. Real phones in one room waits on the M3 reconcile (docs/design/surfaces.md's gap list).
- The Playwright e2e suite is local-only (needs a chromium binary and a spawned wrangler dev); CI runs the workerd suite but not e2e.
- Mid-game team membership is frozen (create/join/kick reject after start-game); full mid-game team lifecycle ships with team mode in M5.
- **Buzz latency compensation is complete on both sides** as of 2026-08-17: the DO ranks by reaction time, the bots are the reference client, and the web surfaces ack the `arm-window`, stamp buzzes with elapsed-since-PAINT, and render the snapshot's `timers`. What has never happened is a measurement over REAL phones on a real venue's Wi-Fi - the A/B below is simulated networks against a local dev room, which is the strongest claim available without the event.
- No real format has ever version-bumped: all portable formats sit at 1.0.0 with empty migration chains (the machinery is exercised by the synthetic example migration only).
- The M0 milestone closes only when the owner's manual hello-world deploy succeeds (ROADMAP exit criteria).

## M4 play surfaces (appended 2026-08-14)

| Claim                                                                                                    | Reproduce                                                                                                                                                                   |
| -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The three play surfaces are real routes over the room-store seam, wired to real rooms since 2026-08-17   | `pnpm dev`, open /room/DUMYX (phone), /room/DUMYX/display, /room/DUMYX/host (`?mirror`, `?theme=<preset>` toggles - DUMYX is the SIMULATION; a created code opens a socket) |
| Store contract: a full fixture game (teams, wagers, final, undo, manual mode) through the action surface | `pnpm -F @jeopardy/web test` (`src/lib/room/room-store.contract.test.ts`)                                                                                                   |
| Every A4 buzzer state, every C4 console state, and the C1b mirror invariant render-tested                | `pnpm -F @jeopardy/web test` (`buzzer-screen.states`, `host-console.states`, `pre-game.states`)                                                                             |
| All four theme presets x all three surfaces smoke-render; QR + room code on the display title screen     | `pnpm -F @jeopardy/web test` (`surfaces-presets.smoke.test.ts`)                                                                                                             |
| Room audio: only-winner-heard exclusive slot (drop, never queue) + serialized sound check                | `pnpm -F @jeopardy/web test` (`room-audio.test.ts`) + the bundled pack gate (`sound-manifest.gate.test.ts`: onset window, duration windows, per-file sha256)                |

Not yet true for M4: the FLIP clue zoom and the host companion view are deferred (docs/design/surfaces.md "Known gaps"). The ws store and the clue-content channel are no longer among them - both landed at the 2026-08-17 reconcile; what is still per-tab is the local SIMULATION, which is what `DUMYX` and `?sim` are.

## Avatars in motion (appended 2026-08-14)

| Claim                                                                                                                                                    | Reproduce                                                                                                                                                                    |
| -------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Three asset tiers ship together and agree with the files on disk: 216 stills (931 KB), 27 walk sheets (568 KB), 27 models + prop + 2 colormaps (2326 KB) | `pnpm -F @jeopardy/web test` (`avatar-manifest.gate.test.ts` re-measures every budget from disk)                                                                             |
| Re-baking without a change is byte-identical                                                                                                             | `pnpm -F @jeopardy/avatar-bake download && pnpm -F @jeopardy/avatar-bake bake && git diff --exit-code apps/web/static/avatars`                                               |
| One recolor implementation serves the bake and the live diorama                                                                                          | `apps/web/src/lib/avatars/palette-recolor.ts` is served type-stripped to the render page (`bake.mjs`, `/shared/palette-recolor.js`); `palette-recolor.test.ts` pins the math |
| The display's diorama is live 3D; the phone's route downloads no renderer                                                                                | `pnpm dev`, open /dev/diorama (fake players, theme switch, buzz beat) and /room/DUMYX/display; `motion-guardrails.gate.test.ts` holds the import rules                       |
| Reduced motion freezes the sheet on frame 0 and stops the wandering                                                                                      | `pnpm -F @jeopardy/web test` (`wander.test.ts` for the crowd; the CSS freeze is a gate-tested explicit state)                                                                |

Not yet true: no Kenney world kit is wired, so every environment except "none" renders as the themed studio ground plane (the slot itself is a real theme-document field since 2026-08-16, and Terra Verde already names `forest`); the diorama has no automated browser test (CI has no chromium - verified by hand, `/dev/diorama` is the standing check).

## The pre-game journey (appended 2026-08-15)

Everything from the front door to the first clue. Decisions: docs/decisions/2026-08-15-staged-lobby.md, plus the 2026-08-15 amendments in docs/decisions/2026-08-14-room-visibility-and-lobby.md and docs/design/user-flows.md A2/A3.

| Claim                                                                                                                                                                                        | Reproduce                                                                                                                                                 |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A room card types the title and the host label as different facts, meters capacity, and prompts for a password inside its own card                                                           | `pnpm -F @jeopardy/web test` (`room-browser.states.test.ts`); the spectator line renders when the wire carries one - absent is "not reported", never zero |
| A full room, or one whose host takes no spectators, refuses in human copy before a character is chosen                                                                                       | `pnpm -F @jeopardy/web test` (`room-settings-surfaces.test.ts`, `room-refusal.ts` - one exhaustive switch over the wire reasons)                          |
| The player route is ONE pre-game screen plus the buzzer, decided by pure functions - a kick empties your team in a region that never went away, a game start moves every phone to the buzzer | `pnpm -F @jeopardy/web test` (`pre-game.states.test.ts`; the four-stage `playerRouteStageFor` was replaced 2026-08-16)                                    |
| The staged lobby places a full room without overlaps, keeps spots stable as teams are created, and makes a team switch a real journey                                                        | `pnpm -F @jeopardy/web test` (`staging/staging-layout.test.ts` - both themes, pure, no GPU)                                                               |
| Recolour is two material writes over one geometry description; a second theme is one data file                                                                                               | read `staging-theme.ts` then `staging-themes/campfires.ts`; live: /dev/diorama, "Staged lobby" + the staging-theme chips                                  |
| Without WebGL the staged lobby degrades to the same layout in CSS (the diorama degrades to nothing; staging may not)                                                                         | `pnpm -F @jeopardy/web test` (`staging/staged-lobby.states.test.ts` renders it server-side, which IS the no-WebGL case)                                   |
| The display works on a phone: scrolling page, width-aware type scale, stage in the flow, scrollable board and scores                                                                         | `pnpm -F @jeopardy/web test` (`display-responsive.gate.test.ts`); live: /room/DUMYX/display in a narrow window                                            |

Not yet true: the staged lobby's 3D half has no automated browser test for the same reason the diorama does not (CI has no chromium); `/dev/diorama` is the standing check. Which staging theme a room uses IS a theme-document field since 2026-08-16 (`?staging=` survives as a dev override).

## The host console after the review (appended 2026-08-17)

The owner's pass over the deployed console. Decision: docs/decisions/2026-08-16-persistent-layout-and-pregame-rework.md ("The console review" addendum); surfaces: docs/design/surfaces.md "Host settings", flows: docs/design/user-flows.md C1b/C1c/C2.

| Claim                                                                                                                                   | Reproduce                                                                                                                                   |
| --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| The console carries a roster rail: every player with team, connection state and score; every team with members, leader, lock and sound  | `pnpm -F @jeopardy/web test` (`room/host-roster-panel.states.test.ts`); live: /room/DUMYX/host                                              |
| The host's powers are real and reach the store: rename, move between teams, take off a team, hand leadership, lock/rename a team, kick  | same file (three decisions against a recording store, then every power against the local sim, incl. a locked team admitting the host)       |
| The host may SEAT a player, not only eject them - and only the host may: `team-join` takes an optional host-only `playerId`             | `pnpm -F @jeopardy/realtime exec vitest run test/teams.test.ts`; shape: `pnpm -F @jeopardy/protocol test` (`room/messages.test.ts`)         |
| Spectators are reported as a count from live connections, and ABSENT stays distinguishable from zero on every surface                   | `pnpm -F @jeopardy/realtime exec vitest run test/teams.test.ts` ("the audience on the roster"); web: `host-roster-panel.states.test.ts`     |
| Neither host panel is painted by the room's theme - fixed `--control-*` chrome, with the type-scale preview as the one themed element   | `pnpm -F @jeopardy/web test` (`host-settings/console-chrome.gate.test.ts` - source-level, because custom properties do not resolve in SSR)  |
| "Save caps" is gone: the caps group states the room's current numbers, spells out the pending edit, and applies only when there is one  | `pnpm -F @jeopardy/web test` (`host-settings/host-settings.test.ts`, "what SAVE CAPS became")                                               |
| Mirror mode is a labelled header toggle carrying its state, and `?mirror` seeds the preference instead of trapping the console in it    | `pnpm -F @jeopardy/web test` (`room/host-console.states.test.ts`); live: /room/DUMYX/host, the "Mirror off" chip                            |
| A room the host created starts EMPTY: the dummy roster reaches the fixture code and `?demo` only, and a local simulation says it is one | `pnpm -F @jeopardy/web test` (`room/create-room-store.test.ts`); live: /room/QWERT/host shows nobody, /room/DUMYX/host shows the dummy room |
| A console that has not heard the room's settings says so rather than drawing the protocol's defaults                                    | `pnpm -F @jeopardy/web test` (`host-settings/host-settings.test.ts`, "a room the console has not heard from")                               |

Every host action above writes through the room store, and since this review was merged onto the reconcile that store is the REAL one: `renamePlayer`, `kickFromRoom`, `kickFromTeam`, `handOffLeadership`, `updateTeam`, `updateRoomSettings` and `assignPlayerToTeam` all send their protocol messages to the DO, which already tested every one of them. Not yet true: there is no spectator LIST and there is not meant to be one - spectators join anonymously and hold no seat, so a count is all that exists.

## The front door, rebuilt (appended 2026-08-18)

One counter, one list, one host button. Decision: docs/decisions/2026-08-18-front-door-architecture.md. Research: docs/research/06-join-flow-patterns.md.

| Claim                                                                                                                                                     | Reproduce                                                                                                                                                                 |
| --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/` has ONE entry field: a complete code arms Join and holds the list back; anything else filters the list live                                           | `pnpm -F @jeopardy/web test` (`room-filter.test.ts` for the rules, `front-door.states.test.ts` for the screen); live: `pnpm dev`                                          |
| A code the list cannot see is treated as the ordinary private room, not as an error - the list stays on screen and the counter says why                   | `room-filter.test.ts` ("does not empty the list for a code it cannot see")                                                                                                |
| The password is a state of having a code - offered for an unlisted code, demanded for a listed locked one, absent for a listed open one                   | `room-filter.test.ts` (`describeCounter`), `front-door.states.test.ts`                                                                                                    |
| Hosting is a button; its form opens inside the counter band and cannot be closed while a creation is in flight, refused, or held                          | `front-door.states.test.ts` ("hosting is a button, and its form opens in place")                                                                                          |
| The masthead is 48px of wordmark strip plus the board gutter (62px measured at 1440px), against ~340px of hero before                                     | `front-door.layout.gate.test.ts` caps the declaration at 3.5rem; measured with a headless 1440x900 and 390x844 pass                                                       |
| The developer index is complete, closed, and lives behind the masthead gear                                                                               | `front-door.states.test.ts` (every `devSurfaces` href is present, `<details>` is not open)                                                                                |
| Nothing on the front door reflows: prose has a measure, headings balance, and every swapping block reserves its height                                    | `pnpm -F @jeopardy/web test` (`front-door.layout.gate.test.ts` - parses every component under lib/landing and lib/lobby)                                                  |
| Creating a public titled room reports back public, with its title and host label, and writes all three into the lobby row                                 | `pnpm -F @jeopardy/web test` (`rooms-endpoint.test.ts`); the console reads them off the room's own `room-settings` message since the reconcile                            |
| The list distinguishes rooms-listed, nothing-matched, nobody-hosting, not-fetched-yet and registry-broken as five states in one reserved block            | `pnpm -F @jeopardy/web test` (`room-browser.states.test.ts`); live with a registry: `pnpm dev:rooms`, http://localhost:8788/                                              |
| The owner's 2026-08-17 copy deletions and field rules survived the rebuild: no listing prose, no pitch footer, name and host required, cap bounds printed | `pnpm -F @jeopardy/web test` (`front-door.states.test.ts` - the deleted-copy gates name every string, by string)                                                          |
| Every surface wears the SAME header bar, and the wordmark is the way home - no stray button                                                               | `pnpm -F @jeopardy/web test` (`front-door.layout.gate.test.ts` holds its height and wordmark scale for every surface; `pnpm/room/pre-game.states.test.ts` for the room's) |
| A host chooses individuals, teams or MIXED when they make the room, and the choice rides on the GAME                                                      | `pnpm -F @jeopardy/web test` (`create-room-request.test.ts`, withPlayerMode; `front-door.states.test.ts` for the control)                                                 |
| Mixed seats teams and soloists in one game, each scoring as itself, and invents a team for nobody                                                         | `pnpm -F @jeopardy/engine test` (`lobby.test.ts`, matrix #34); the two questions themselves: `pnpm -F @jeopardy/protocol test` (`settings/groups/teams.test.ts`)          |
| A host whose TAB died gets the room back: the key survives the tab and expires with the room                                                              | `pnpm -F @jeopardy/web test` (`join-hand-off.test.ts`)                                                                                                                    |
| The two Worker configs describe ONE system - same D1 id, same DO class, cross-script binding by name                                                      | `pnpm -F @jeopardy/web test` (`realtime/worker-config.gate.test.ts`) - a dry-run deploy cannot catch a mismatch                                                           |
| A phone reaching a PASSWORD room by URL alone can type the password, on the socket it already has                                                         | `pnpm -F @jeopardy/web test` (`pre-game.states.test.ts`, "the password door")                                                                                             |
| A host chooses WHICH GAME: the built-in sample, the club night, or a .game.json they bring                                                                | `pnpm -F @jeopardy/web test` (`game-catalog.test.ts` against the real event documents, `front-door.states.test.ts` for the control)                                       |
| The club night's game is served by this app and joined to its pack in the browser before hosting                                                          | same file ("loading the club night's game"); live: create a room and pick it                                                                                              |
| Clue media RENDERS: pictures, sound, video and any other file, on the display and the host card                                                           | `pnpm -F @jeopardy/web test` (`clue-media.states.test.ts`, `host-console.states.test.ts`); live: /room/DUMYX/host, cell (0,0)                                             |
| The room RESOLVES a media id before sending it - kind, type, alt and a url when the bytes are fetchable                                                   | `pnpm -F @jeopardy/realtime exec vitest run test/host-controls.test.ts` ("clue content on the wire")                                                                      |
| An asset with no fetchable bytes shows its alt text, never a broken frame                                                                                 | same two files - the `pending-local` case is asserted on both sides                                                                                                       |
| The wire carries the seating MODE, and a snapshot that omits it fails loudly rather than defaulting                                                       | `pnpm -F @jeopardy/protocol test` (`room/messages.test.ts`); `pnpm -F @jeopardy/realtime exec vitest run test/client-state.test.ts`                                       |
| The animated character never shows colour fringes: the recolor skips transparent pixels and the strip is re-encoded losslessly                            | `pnpm -F @jeopardy/web test` (`palette-recolor.test.ts`, the transparent-gutter cases); live: /room/DUMYX and flip through the accents                                    |
| A game definition can be joined to its content pack, and a stale or wrong pack is refused by name                                                         | `pnpm -F @jeopardy/protocol test` (`modes/jeopardy/compose.test.ts` - against the real event documents)                                                                   |
| Freshness is a wall-clock stamp, not "just now": the line is true without a ticking clock behind it                                                       | `pnpm -F @jeopardy/web test` (`room-browser.states.test.ts`, "freshness is a clock time, not a phrase")                                                                   |
